Release notes for each version live here as HTML fragments named to match
the archive (LiveManager-1.2.html next to LiveManager-1.2.zip).

Write them as fragments — no <!DOCTYPE>, <html> or <body> — so generate_appcast
embeds them directly into the appcast.
